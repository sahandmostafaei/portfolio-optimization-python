# Data Directory

This folder is reserved for optional cached market data.

By default, the project downloads historical stock prices directly from Yahoo Finance using the `yfinance` package.

No CSV files are required for normal execution.

If desired, downloaded datasets can be saved here for faster future runs.
